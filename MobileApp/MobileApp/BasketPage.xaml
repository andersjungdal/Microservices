<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MobileApp.BasketPage">
    <!--<ContentPage.BindingContext>-->
    
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <CollectionView ItemsSource="{Binding}" SelectionMode="Single" SelectionChanged="Product_Select" >
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding Name}" FontSize="Large"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
            
            <RelativeLayout HorizontalOptions="FillAndExpand" VerticalOptions="Start">

                <Button Text="Remove selected items" Clicked="RemoveSelectedItems" 
                        RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=.40}"/>

            </RelativeLayout>


        </StackLayout>
    </ContentPage.Content>
    
    <!--</ContentPage.BindingContext>-->
</ContentPage>